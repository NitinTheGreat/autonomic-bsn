"""Phase 3 — failure injection lab.

Each injector is a DataSource decorator: it wraps a DataSource and is itself a
valid DataSource, so injectors compose with each other and with Phase 9's
HardwareLiveSource unchanged.
"""
